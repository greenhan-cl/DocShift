# DocShift Mock Server

This standard-library Python server provides in-memory sample data for local frontend development. No installation step is required.

```bash
cd mock_server
python app.py
```

It listens on `http://127.0.0.1:8080`, matching the default Vite `/api` proxy. Restart it to reset the sample Task and conversion items.
